# MANAGED BY PUPPET

<%- parameters.sort_by { |k,v| k }.each |kk,vv| do -%>
<%= kk %> <%= vv %>
<%- end -%>

# Debuntu has an init script that populates nss_initgroups_ignoreusers with all local accounts
# If the custom fact is installed to emulate this, the same will be put here
<%- if @nss_initgroups_ignoreusers != nil -%>
nss_initgroups_ignoreusers <%= @nss_initgroups_ignoreusers %>
<%- end -%>
